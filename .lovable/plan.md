Add a temporary `console.error` call as the very first statement inside the `catch` block in `src/pages/Auth.tsx` (currently starting at line 97). This will log the full error object and related diagnostic fields (`navigator.onLine`, `typeof error`, `instanceof TypeError`, `message`, `code`, `status`, `name`) before any existing network-error or login-failure logic runs.

No other files or logic will be changed.
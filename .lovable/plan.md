# Test invoice for Dublin Gas outstanding-invoice reminder

Create one test job/invoice in the Dublin Gas account that matches the outstanding-invoice-reminder criteria, so the Make scenario can be tested end to end.

## What gets created

A single completed job for the existing Dublin Gas customer **Abdenneur9** (phone +353 87 235 4257, not opted out), with:

- Status: Completed, completed 21 days ago (inside the 14–60 day window)
- Payment method: Invoice, payment status: Unpaid
- Balance due: €185.00
- Invoice number: INV-2026-TEST1, invoiced on the completion date
- Reminder count: 0 (so the first reminder is eligible)
- Job type: Repair, notes marked as a test record

Nothing else is modified — no existing jobs, customers, or settings change.

## Why this customer

Dublin Gas has no existing job that meets the criteria (all completed jobs are paid or have no payment method), so a new record is needed. Abdenneur9 is an existing test customer on the Dublin Gas org with a working phone number and reminders not opted out, which is what the reminder flow requires.

## Technical details

- Insert one row into `service_calls` with `organisation_id = f1950683-...` (Dublin Gas), `customer_id = 2f8220d9-335a-4328-92d2-618d92e30310`, `user_id = a129f160-...`, `payment_status = 'unpaid'`, `payment_method = 'invoice'`, `status = 'Completed'`, `completed_at = now() - 21 days`, `invoiced_at = now() - 21 days`, `balance_due = 185`, `revenue = 185`, `invoice_reminder_count = 0`.
- `job_reference` is auto-generated by the existing trigger (next DG-### in sequence).
- After insert, verify by running the same filter used by `get-outstanding-invoices` / `send-outstanding-invoice-reminders` and report back the job reference, invoice number, customer, phone, balance and completion date.

## Cleanup

Say the word after testing and the test job will be deleted.
